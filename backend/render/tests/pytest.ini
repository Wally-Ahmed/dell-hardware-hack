; Local pytest config so `pytest backend/render/tests/` is self-contained
; regardless of what lands at the repo root today (same move as agent/tests).
[pytest]
asyncio_default_fixture_loop_scope = function
