Two things make that door real. Every person we detect lives in a MongoDB collection on the box: their policy, their consent record, their face embedding. And the agent runs under NVIDIA's NemoClaw stack, with an egress allowlist you can read on screen: our backend, the local model, and nothing else.
